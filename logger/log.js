const chalk = require("chalk");

module.exports = {
  log(data) {
    const types = {
      0: "info",
      1: "success",
      2: "failed",
      3: "error",
      4: "warning",
    }

    const who = data.who

    
    

  },
};
